# Canceled

**What this is:** Tasks that were dropped. Not deleted — kept for context.

**When a task lands here:**
- Append a `## Cancellation` section explaining why.
- Update `status` to `canceled`, `moved_at` to today.

**These may be revisited later.** If reactivated, move back to `Cooking/` and update status.
