# InProgress

Active work. Each folder here has a Claude Code instance running (or ready to spawn).

Each task folder contains:
- **SPEC.md** — what to build
- **CLAUDE.md** — instructions for the executor
- **STATE.md** — progress checkpoint (updated after each milestone)

Branch exists on origin. Commits are incremental.
